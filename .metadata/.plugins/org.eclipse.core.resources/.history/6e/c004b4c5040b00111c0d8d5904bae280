html {
  font-size: 16px;
  line-height: 1.5;
  -webkit-text-size-adjust: 100%;
}

body {
  margin: 0;
  padding: 0;
  font-family: Arial, sans-serif;
}

/* Reset */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html, body, #root {
  width: 100%;
}

/* ================= HERO SECTION ================= */
.hero {
  width: 100%;
  height: 82vh;
  background: url("https://images.unsplash.com/photo-1566073771259-6a8506099945")
              no-repeat center center;
  background-size: cover;
  color: white;
  position: relative;
}

/* Dark overlay */
.hero::before {
  content: "";
  position: absolute;
  inset: 0;
  background: rgba(0,0,0,0.5);
}

.hero > * {
  position: relative;
  z-index: 1;
}

/* ================= NAVBAR ================= */
.navbar {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  z-index: 1000;

  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 10px 60px;

  backdrop-filter: blur(10px);
  background: linear-gradient(
    to bottom,
    rgba(0,0,0,0.85),
    rgba(0,0,0,0.6)
  );
}

/* Navbar links */
.navbar ul {
  list-style: none;
  display: flex;
  gap: 25px;
}

.navbar li {
  padding: 8px 14px;
  border-radius: 6px;
  cursor: pointer;
  transition: 0.3s;
}

.navbar li:hover {
  background: white;
  color: #8b5e3c;
}

/* Login button */
.nav-login-btn {
  background: #8b5e3c;
  color: #fff;
  border: none;
  padding: 8px 18px;
  border-radius: 6px;
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
}

/* ================= HERO CONTENT ================= */
.hero-content {
  width: 100%;
  max-width: 1200px;
  margin: 0 auto;
  padding-top: 70px;

  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 30px;
}

.left {
  width: 45%;
}

.left h1 {
  font-size: 65px;
  margin-bottom: 20px;
}

.left p {
  margin-bottom: 20px;
  opacity: 0.9;
}

/* ================= BOOKING CARD ================= */
.booking-card {
  width: 380px;
  background: #ffffff;
  color: #333;
  padding: 25px;
  border-radius: 6px;
  display: flex;
  flex-direction: column;
  gap: 25px;
  box-shadow: 0 8px 25px rgba(0,0,0,0.25);
}

.date-row, .row {
  display: flex;
  gap: 10px;
}

.field {
  display: flex;
  flex-direction: column;
  flex: 1;
}

.field input,
.field select {
  padding: 10px;
  border-radius: 4px;
  border: 1px solid #ddd;
}

/* ================= ROOMS SECTION ================= */

.rooms {
  background: #f5f0ea;
  padding: 90px 80px;
  text-align: center;
}

/* heading */
.rooms-tag {
  color: #8b5e3c;
  font-size: 13px;
  letter-spacing: 2px;
  text-transform: uppercase;
}

.rooms-heading {
  font-size: 42px;
  margin: 10px 0 15px;
  font-weight: 700;
  color: #2b2b2b;
}

.rooms-subtext {
  max-width: 720px;
  margin: 0 auto 60px;
  color: #777;
  font-size: 14px;
  line-height: 1.7;
}

/* GRID */
.lux-rooms-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 32px;
}

/* CARD */
.lux-room-card {
  position: relative;
  height: 380px;
  border-radius: 22px;
  overflow: hidden;
  box-shadow: 0 18px 40px rgba(0,0,0,0.18);
  cursor: pointer;
}

/* IMAGE */
.lux-room-card img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.6s ease;
}

/* OVERLAY */
.lux-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(
    to top,
    rgba(0,0,0,0.75),
    rgba(0,0,0,0.25)
  );
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
  align-items: center;
  padding: 30px;
  text-align: center;
  color: #fff;
}

/* TEXT */
.lux-overlay h3 {
  font-size: 22px;
  font-weight: 600;
  margin-bottom: 6px;
}

.lux-overlay p {
  font-size: 15px;
  margin-bottom: 16px;
}

/* BUTTON */
.lux-overlay button {
  background: #8b5e3c;
  color: #fff;
  border: none;
  padding: 10px 24px;
  border-radius: 25px;
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  opacity: 1;
  transition: all 0.3s ease;
}

.lux-overlay button:hover {
  background: #6f472d;
}

/* HOVER EFFECT */
.lux-room-card:hover img {
  transform: scale(1.1);
}

/* RESPONSIVE */
@media (max-width: 1024px) {
  .lux-rooms-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (max-width: 640px) {
  .lux-rooms-grid {
    grid-template-columns: 1fr;
  }
}

/* ================= VIEW ALL ROOMS BUTTON ================= */

.view-all-wrapper {
  margin-top: 50px;
  display: flex;
  justify-content: center;
}

/* BUTTON */
.view-all-btn {
  background: #8b5e3c;                /* brown fill */
  color: #fff;
  border: none;
  padding: 14px 38px;
  border-radius: 999px;               /* pill shape */
  font-size: 14px;
  font-weight: 600;
  letter-spacing: 0.4px;
  cursor: pointer;

  /* BLUE GLOW RING */
  box-shadow:
    0 0 0 3px rgba(88, 151, 255, 0.6),
    0 10px 25px rgba(139, 94, 60, 0.35);

  transition: all 0.35s ease;
}

/* HOVER */
.view-all-btn:hover {
  background: #6f472d;
  transform: translateY(-3px);

  box-shadow:
    0 0 0 4px rgba(88, 151, 255, 0.85),
    0 14px 35px rgba(139, 94, 60, 0.45);
}

/* ACTIVE (CLICK) */
.view-all-btn:active {
  transform: translateY(0);
}



/* ================= EXPLORE ================= */
/* EXPLORE SECTION FINAL UI  */
/* ========================= */

.explore {
  background:#ffffff;
  padding: 100px 80px;
  text-align: center;
   margin-top: -60px;
   padding-top: 40px;
}

/* small heading */
.explore-tag {
  color: #8b5e3c;        /* brown luxury color */
  font-size: 16px;       /* increased size */
  font-weight: 700;      /* bold */
  letter-spacing: 4px;   /* spacing between letters */
  text-transform: uppercase;
  margin-bottom: 12px;
}
.explore-tag::after {
  content: "";
  display: block;
  width: 40px;
  height: 2px;
  background: #8b5e3c;
  margin: 8px auto 0;
}


/* main heading */
.explore-heading {
  font-size: 44px;
  margin: 10px 0 15px;
  font-weight: 700;
  color: #2b2b2b;
}

/* paragraph */
.explore-subtext {
  max-width: 720px;
  margin: 0 auto 60px;
  color: #777;
  font-size: 14px;
  line-height: 1.7;
}

/* GRID LAYOUT */
.explore-grid {
  display: grid;
  grid-template-columns: repeat(3, 280px); /* 👈 fixed smaller width */
  justify-content: center;                 /* center all cards */
  gap: 30px;
}

/* CARD */
.explore-card {
  background: #f5f0ea;;
  padding: 45px 30px;
  border-radius: 6px;
  border: 1px solid #eee;
  height: 230px;              /* 👈 increased height */
  width: 280px;               /* 👈 fixed width (square look) */
  transition: all 0.35s ease;
  position: relative;
}


/* ICON */
.explore-icon {
  font-size: 34px;       /* 🔥 bigger icon */
  color: #8b5e3c;
  margin-bottom: 20px;
}

/* TITLE */
.explore-card h3 {
  font-size: 18px;
  margin-bottom: 10px;
  font-weight: 600;
  color: #222;
}

/* TEXT */
.explore-card p {
  font-size: 13px;
  color: #777;
  margin-bottom: 12px;
  line-height: 1.6;
}

/* LINK */
.explore-card a {
  font-size: 13px;
  color: #8b5e3c;
  text-decoration: none;
  font-weight: 500;
}

/* ⭐ PREMIUM HOVER EFFECT */
.explore-card:hover {
  transform: translateY(-10px);
  box-shadow: 0 20px 40px rgba(0,0,0,0.08);
  border-color: #e5d6c6;
}

/* RESPONSIVE */
@media (max-width: 900px) {
  .explore-grid {
    grid-template-columns: 1fr;
  }
}

/* ================= TESTIMONIALS ================= */
.testimonials {
  background: #f5f0ea;
  padding: 90px 80px;
  text-align: center;
}

/* ================= GALLERY ================= */
.gallery {
  background: #ffffff;
  padding: 90px 80px;
  text-align: center;
}

/* ================= ABOUT ================= */
.about {
  background: #f5f0ea;
  padding: 90px 80px;
}

/* ================= FOOTER ================= */
.footer {
  background: #1b1b1b;
  color: #ccc;
  padding-top: 70px;
}

.footer-bottom {
  text-align: center;
  padding: 15px;
  background: #111;
  font-size: 13px;
}
