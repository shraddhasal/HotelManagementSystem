html {
  font-size: 16px;
  line-height: 1.5;
  -webkit-text-size-adjust: 100%;
}

body {
  margin: 0;
  padding: 0;
  font-family: Arial, sans-serif;
}



/* Reset */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: Arial, sans-serif;
}

body, html, #root {
  width: 100%;
  height: 100%;
}

/* HERO SECTION */
.hero {
  width: 100%;
  height: 82vh;
  background: url("https://images.unsplash.com/photo-1566073771259-6a8506099945")
              no-repeat center center;
  background-size: cover;
  color: white;
  position: relative;
  margin-bottom: 0;
  
}

/* Dark overlay */
.hero::before {
  content: "";
  position: absolute;
  inset: 0;
  background: rgba(0,0,0,0.5);
}

.hero > * {
  position: relative;
  z-index: 1;
}

/* NAVBAR */
.navbar {
  position: fixed;      /* 🔥 always fixed on top */
  top: 0;
  left: 0;
  width: 100%;
  z-index: 1000;

  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 10px 60px;

  backdrop-filter: blur(10px);
  background: linear-gradient(
    to bottom,
    rgba(0,0,0,0.85),
    rgba(0,0,0,0.6)
  );
  border-bottom: 1px solid rgba(255,255,255,0.1);
}


.navbar ul {
  list-style: none;
  display: flex;
  gap: 25px;
}

.navbar li {
  padding: 8px 14px;
  border-radius: 6px;
  cursor: pointer;
  transition: 0.3s;
}

/* Hover effect */
.navbar li:hover {
  background: white;
  color: #8b5e3c;
}

/* ================= NAVBAR LOGIN BUTTON ================= */
.nav-login-btn {
  background: #8b5e3c;      /* brown */
  color: #fff;
  border: none;
  padding: 8px 18px;
  border-radius: 6px;
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  height: 38px;

  display: flex;
  align-items: center;
  justify-content: center;

  transition: all 0.3s ease;
}

/* Hover */
.nav-login-btn:hover {
  background: #6f472d;
  transform: translateY(-1px);
}

/* REMOVE browser default button styles */
.nav-login-btn:focus {
  outline: none;
}

/* HERO CONTENT */
.hero-content {
  width: 100%;
  max-width: 1200px;      /* 🔥 this centers content */
  margin: 0 auto;         /* center horizontally */
  height: calc(100vh - 70px);

  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 30px;
}


/* LEFT TEXT */
.left {
  width: 45%;
}

.left h1 {
  font-size: 65px;
  font-weight: bold;
  margin-bottom: 20px;
}

.left p {
  margin-bottom: 20px;
  opacity: 0.9;
  max-width: 500px;
}

/* BOOKING CARD RIGHT SIDE */
.booking-card {
  width: 380px;
  background: #ffffff;
  color: #333;
  padding: 25px;
  border-radius: 6px;        /* 🔥 very less curve */
  display: flex;
  flex-direction: column;
  gap: 25px;
  box-shadow: 0 8px 25px rgba(0,0,0,0.25);
}

/* Heading */
.booking-card h2 {
  text-align: left;
  margin-bottom: 10px;
}

/* Row layout */
.date-row {
  display: flex;
  gap: 10px;
}

.row {
  display: flex;
  gap: 10px;
}

/* Field */
.field {
  display: flex;
  flex-direction: column;
  flex: 1;
}

.field label {
  font-size: 13px;
  margin-bottom: 5px;
  color: #444;
}

/* Inputs */
.field input,
.field select {
  padding: 10px;
  border-radius: 4px;      /* 🔥 almost square */
  border: 1px solid #ddd;
  font-size: 14px;
  outline: none;
}

/* Button */
.check-btn {
  background: #8b5e3c;
  color: white;
  padding: 12px;
  border: none;
  cursor: pointer;
  border-radius: 4px;
  font-weight: bold;
  transition: 0.3s;
}

.check-btn:hover {
  background: #6f472d;
}

/* ========================= */
/* EXPLORE SECTION FINAL UI  */
/* ========================= */

.explore {
  background:#ffffff;
  padding: 100px 80px;
  text-align: center;
   margin-top: -60px;
   padding-top: 40px;
}

/* small heading */
.explore-tag {
  color: #8b5e3c;        /* brown luxury color */
  font-size: 16px;       /* increased size */
  font-weight: 700;      /* bold */
  letter-spacing: 4px;   /* spacing between letters */
  text-transform: uppercase;
  margin-bottom: 12px;
}
.explore-tag::after {
  content: "";
  display: block;
  width: 40px;
  height: 2px;
  background: #8b5e3c;
  margin: 8px auto 0;
}


/* main heading */
.explore-heading {
  font-size: 44px;
  margin: 10px 0 15px;
  font-weight: 700;
  color: #2b2b2b;
}

/* paragraph */
.explore-subtext {
  max-width: 720px;
  margin: 0 auto 60px;
  color: #777;
  font-size: 14px;
  line-height: 1.7;
}

/* GRID LAYOUT */
.explore-grid {
  display: grid;
  grid-template-columns: repeat(3, 280px); /* 👈 fixed smaller width */
  justify-content: center;                 /* center all cards */
  gap: 30px;
}

/* CARD */
.explore-card {
  background: #f5f0ea;;
  padding: 45px 30px;
  border-radius: 6px;
  border: 1px solid #eee;
  height: 230px;              /* 👈 increased height */
  width: 280px;               /* 👈 fixed width (square look) */
  transition: all 0.35s ease;
  position: relative;
}


/* ICON */
.explore-icon {
  font-size: 34px;       /* 🔥 bigger icon */
  color: #8b5e3c;
  margin-bottom: 20px;
}

/* TITLE */
.explore-card h3 {
  font-size: 18px;
  margin-bottom: 10px;
  font-weight: 600;
  color: #222;
}

/* TEXT */
.explore-card p {
  font-size: 13px;
  color: #777;
  margin-bottom: 12px;
  line-height: 1.6;
}

/* LINK */
.explore-card a {
  font-size: 13px;
  color: #8b5e3c;
  text-decoration: none;
  font-weight: 500;
}

/* ⭐ PREMIUM HOVER EFFECT */
.explore-card:hover {
  transform: translateY(-10px);
  box-shadow: 0 20px 40px rgba(0,0,0,0.08);
  border-color: #e5d6c6;
}

/* RESPONSIVE */
@media (max-width: 900px) {
  .explore-grid {
    grid-template-columns: 1fr;
  }
}
/* ================= ROOMS SECTION ================= */

.rooms {
  background: #f5f0ea;    /* 🔥 different from explore (#f6f2ee) */
  padding: 60px 80px 80px;
  text-align: center;
   padding-top: 40px;
   margin-top: -60px;
}

/* small tag */
.rooms-tag {
  color: #8b5e3c;
  font-size: 13px;
  letter-spacing: 2px;
  text-transform: uppercase;
}

/* main heading */
.rooms-heading {
  font-size: 42px;
  margin: 10px 0 15px;
  font-weight: 700;
  color: #2b2b2b;
}

/* paragraph */
.rooms-subtext {
  max-width: 720px;
  margin: 0 auto 60px;
  color: #777;
  font-size: 14px;
  line-height: 1.7;
}

/* ===== HOMEPAGE LUXURY ROOMS GRID ===== */
.lux-rooms-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 32px;
  margin-top: 50px;
}

/* ===== LUXURY ROOM CARD ===== */
.lux-room-card {
  position: relative;
  height: 380px;
  border-radius: 18px;
  overflow: hidden;
  cursor: pointer;
  box-shadow: 0 14px 40px rgba(0, 0, 0, 0.18);
}

.lux-room-card img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.6s ease;
}

/* ===== OVERLAY ===== */
.lux-overlay {
  position: absolute;
  inset: 0;
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
  padding: 24px;
  background: linear-gradient(
    to top,
    rgba(0, 0, 0, 0.75),
    rgba(0, 0, 0, 0.15)
  );
  color: #fff;
}

.lux-overlay h3 {
  font-size: 22px;
  font-weight: 600;
  margin-bottom: 6px;
}

.lux-overlay p {
  font-size: 16px;
  margin-bottom: 14px;
}

.lux-overlay button {
  align-self: flex-start;
  background: #8b5e3c;
  color: #fff;
  border: none;
  padding: 10px 22px;
  border-radius: 25px;
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  opacity: 0;
  transform: translateY(10px);
  transition: all 0.35s ease;
}

/* ===== HOVER EFFECT ===== */
.lux-room-card:hover img {
  transform: scale(1.12);
}

.lux-room-card:hover .lux-overlay button {
  opacity: 1;
  transform: translateY(0);
}

/* ================= VIEW ALL ROOMS BUTTON ================= */

.view-all-wrapper {
  margin-top: 50px;
  display: flex;
  justify-content: center;
}

.view-all-btn {
  background: transparent;
  border: 2px solid #8b5e3c;
  color: #8b5e3c;
  padding: 14px 36px;
  border-radius: 30px;
  font-size: 14px;
  font-weight: 600;
  letter-spacing: 0.5px;
  cursor: pointer;
  transition: all 0.35s ease;
}

/* Hover effect */
.view-all-btn:hover {
  background: #8b5e3c;
  color: #fff;
  transform: translateY(-3px);
  box-shadow: 0 10px 25px rgba(139, 94, 60, 0.35);
}


/* ===== RESPONSIVE ===== */
@media (max-width: 1024px) {
  .lux-rooms-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (max-width: 640px) {
  .lux-rooms-grid {
    grid-template-columns: 1fr;
  }
}

/* ================= TESTIMONIALS ================= */

.testimonials {
  background: #f5f0ea;   /* 🔥 different soft background */
  padding: 90px 80px;
  text-align: center;
}

/* small heading */
.test-tag {
  color: #8b5e3c;
  font-size: 13px;
  letter-spacing: 2px;
  text-transform: uppercase;
}

/* main heading */
.test-heading {
  font-size: 40px;
  margin: 10px 0 50px;
  font-weight: 700;
  color: #2b2b2b;
}

/* GRID */
.test-grid {
  display: grid;
  grid-template-columns: repeat(3, 320px);
  justify-content: center;
  gap: 30px;
}

/* CARD */
.test-card {
  background: #ffffff;
  padding: 30px 25px;
  border-radius: 8px;
  box-shadow: 0 8px 20px rgba(0,0,0,0.06);
  transition: 0.3s;
  text-align: center;
}

/* USER IMAGE */
.test-card img {
  width: 70px;
  height: 70px;
  border-radius: 50%;
  margin-bottom: 15px;
}

/* TEXT */
.test-card p {
  font-size: 14px;
  color: #666;
  line-height: 1.6;
  margin-bottom: 15px;
}

/* NAME */
.test-card h4 {
  font-size: 16px;
  color: #222;
  margin-bottom: 5px;
}

/* ROLE */
.test-card span {
  font-size: 13px;
  color: #8b5e3c;
}

/* HOVER */
.test-card:hover {
  transform: translateY(-8px);
  box-shadow: 0 15px 35px rgba(0,0,0,0.1);
}


/* ================= GALLERY ================= */

.gallery {
  background: #ffffff;   /* different from testimonials */
  padding: 90px 80px;
  text-align: center;
  margin-top: -60px;
   padding-top: 40px;
}

/* small tag */
.gallery-tag {
  color: #8b5e3c;
  font-size: 13px;
  letter-spacing: 2px;
  text-transform: uppercase;
}

/* main heading */
.gallery-heading {
  font-size: 40px;
  margin: 10px 0 50px;
  font-weight: 700;
  color: #2b2b2b;
}

/* GRID */
.gallery-grid {
  display: grid;
  grid-template-columns: repeat(3, 320px);
  justify-content: center;
  gap: 25px;
}

/* IMAGE CARD */
.gallery-item {
  position: relative;
  overflow: hidden;
  border-radius: 6px;
  cursor: pointer;
}

/* IMAGE */
.gallery-item img {
  width: 100%;
  height: 220px;
  object-fit: cover;
  transition: transform 0.5s ease;
}

/* HOVER ZOOM */
.gallery-item:hover img {
  transform: scale(1.1);
}

/* OVERLAY */
.overlay {
  position: absolute;
  inset: 0;
  background: rgba(0,0,0,0.45);
  color: white;
  font-size: 16px;
  font-weight: 500;
  display: flex;
  justify-content: center;
  align-items: center;
  opacity: 0;
  transition: 0.4s;
}

/* SHOW OVERLAY ON HOVER */
.gallery-item:hover .overlay {
  opacity: 1;
}

/* ================= FOOTER ================= */

.footer {
  background: #1b1b1b;
  color: #ccc;
  padding-top: 70px;
}

/* Container */
.footer-container {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 40px;
  padding: 0 80px 50px;
}

/* Logo */
.footer-logo {
  color: #fff;
  font-size: 26px;
  margin-bottom: 15px;
}

/* Headings */
.footer-col h3 {
  color: #fff;
  margin-bottom: 15px;
}

/* Text */
.footer-col p {
  font-size: 14px;
  line-height: 1.6;
  color: #bbb;
}

/* Links */
.footer-col ul {
  list-style: none;
  padding: 0;
}

.footer-col li {
  margin-bottom: 10px;
  cursor: pointer;
  transition: 0.3s;
}

.footer-col li:hover {
  color: #c8a97e;
}

/* Subscribe */
.subscribe-box {
  display: flex;
  margin-top: 10px;
}

.subscribe-box input {
  padding: 10px;
  border: none;
  outline: none;
  flex: 1;
  font-size: 13px;
}

.subscribe-box button {
  background: #c8a97e;
  border: none;
  padding: 10px 15px;
  color: #fff;
  cursor: pointer;
  transition: 0.3s;
}

.subscribe-box button:hover {
  background: #a8825e;
}

/* Social icons */
.social-icons {
  margin-top: 15px;
  display: flex;
  gap: 12px;
  font-size: 18px;
}

.social-icons span {
  cursor: pointer;
  transition: 0.3s;
}

.social-icons span:hover {
  color: #c8a97e;
}

/* Bottom bar */
.footer-bottom {
  text-align: center;
  padding: 15px;
  background: #111;
  font-size: 13px;
  color: #888;
}

/* ================= ABOUT SECTION ================= */

.about {
  background: #f5f0ea; /* different from explore */
  padding: 90px 80px;
}

/* layout */
.about-container {
  max-width: 1100px;      /* control total width */
  margin: 0 auto;         /* 🔥 center whole section */
  
  display: flex;
  align-items: center;    /* vertical center */
  justify-content: center;/* horizontal center */

  gap: 60px;
}

/* LEFT IMAGE GRID */
.about-images {
  display: grid;
  grid-template-columns: repeat(2, 200px);
  grid-template-rows: repeat(2, 150px);
  gap: 15px;
  justify-content: center;  /* 🔥 center image block */
}


.about-images img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  border-radius: 6px;
}

/* RIGHT CONTENT */
.about-content {
  max-width: 450px;
}


/* tag */
.about-tag {
  color: #8b5e3c;
  font-size: 14px;
  letter-spacing: 2px;
  text-transform: uppercase;
}

/* heading */
.about-content h2 {
  font-size: 40px;
  margin: 10px 0 20px;
  color: #222;
}

/* paragraph */
.about-content p {
  color: #666;
  font-size: 15px;
  line-height: 1.7;
  margin-bottom: 15px;
}

/* button */
.about-btn {
  background: #8b5e3c;
  color: white;
  border: none;
  padding: 12px 25px;
  cursor: pointer;
  margin-top: 10px;
  transition: 0.3s;
}

.about-btn:hover {
  background: #6f472d;
}


/* RESPONSIVE */
@media (max-width: 768px) {
  .hero-content {
    flex-direction: column;
    text-align: center;
    gap: 30px;
  }

  .left {
    width: 100%;
  }

  .booking-card {
    width: 100%;
  }

  .navbar {
    flex-direction: column;
    gap: 10px;
  }

  
}
