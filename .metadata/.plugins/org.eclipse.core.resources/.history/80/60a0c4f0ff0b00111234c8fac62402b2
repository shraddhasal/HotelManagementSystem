/* ================= NAVBAR FIX ================= */
.navbar {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  z-index: 1000;

  display: flex;
  align-items: center;
  justify-content: space-between;

  padding: 12px 40px;   /* reduced to avoid overflow */

  background: linear-gradient(
    to bottom,
    rgba(0,0,0,0.9),
    rgba(0,0,0,0.75)
  );

  box-sizing: border-box;
  overflow: hidden;    /* 🔥 prevents button escape */
}

/* LOGO */
.navbar .logo {
  color: #fff;
  font-size: 22px;
  font-weight: 700;
  white-space: nowrap;
}

/* CENTER MENU */
.navbar ul {
  list-style: none;
  display: flex;
  gap: 26px;
  margin: 0;
  padding: 0;
  flex-wrap: nowrap;
}

.navbar li {
  color: #fff;
  padding: 8px 14px;
  border-radius: 6px;
  cursor: pointer;
  transition: 0.3s;
  white-space: nowrap;
}

.navbar li:hover {
  background: #fff;
  color: #8b5e3c;
}

/* RIGHT SECTION */
.nav-right {
  display: flex;
  align-items: center;
  gap: 12px;
  white-space: nowrap;
  flex-shrink: 0;     /* 🔥 DO NOT SHRINK */
}

/* USER TEXT */
.welcome-text {
  color: #fff;
  font-size: 14px;
}

/* LOGIN / LOGOUT BUTTON */
.nav-login-btn {
  background: #8b5e3c;
  color: #fff;
  border: none;
  padding: 8px 18px;
  border-radius: 6px;
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  white-space: nowrap;
}

.nav-login-btn:hover {
  opacity: 0.9;
}
